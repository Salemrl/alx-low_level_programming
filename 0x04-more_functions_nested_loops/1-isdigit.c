#include "main.h"

/**
 * _isdigit - will check if digit
 * @n: digit to be checked
 * Return: 1 if digit otherwise 0
 */

int _isdigit(int n)
{
	if (n >= '1' && n <= '9')
		Return (1);

	return (0);
}
